test.classList.method()
// method 자리에는 들어갈 수 있는 것들
// add(string) - 지정한 class를 추가한다.
// remove(string) - 지정한 class를 삭제한다
// item(number) - 특정 index 값에 있는 class를 반환한다
// toggle(string) - class가 이미 존재한다면 제거하고 false를 반환 / class가 존재하지 않았다면 class를 추가하고 true를 반환
// contains(string) - 지정한 class가 존재하는지 확인한다
// replace(oldClass, newClass) - 존재하던 oldClass를 제거하고 새로운 newClass로 교체한다